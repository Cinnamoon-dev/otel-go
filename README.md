# Opentelemetry Golang
A simple instrumentation using Opentelemetry (Grafana Alloy) and the Grafana stack (Tempo, Loki and Prometheus).

## Project Structure
The idea here is that the API runs on the local machine and the stack runs in containers with port fowarding to
the local machine.
